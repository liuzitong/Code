【 内存收缩策略 】
   有一段内存长度为 N, 每次都会使用 n 长度（n 为变化 )。如何确保一段最小的 M 满足
一段时间内不会出现 n > M 的情况？

   1）1/2 递减法
   定义 块 BLK 为对齐大小
   定义 C 为当前使用长度, RC 为 对齐 C 到 BLK上。
   
   条件：如果 !( RC * 2 + BLK < N ) 且  N > BLK, 表示可以执行减缩
   减缩：N / 2 缩减

   
 